server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # Ruta del repositorio (config-repo)
          search-locations: file:../config-repo
